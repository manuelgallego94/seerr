services:
  seerr:
    container_name: seerFAV
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - 5057:5055
    volumes:
      - .:/app:rw,cached
      - /app/node_modules
      - /app/.next
